name: Issues workflow
on: workflow_dispatch
jobs: 
  info:
    runs-on: ubuntu-latest
    steps: 
      - name: Output Github context
        run: | 
          echo "Repository ${{ github.repository }}"
          echo "Author ${{ github.repository_owner }}"
          echo "Event info ${{ github.event }}"
